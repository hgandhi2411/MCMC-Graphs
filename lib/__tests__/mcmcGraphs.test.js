const assert = require('assert');
// Const mcmcGraphs = require('../index.js');

describe('mcmcGraphs', () => {
  it('has a test', () => {
    assert(false, 'mcmcGraphs should have a test');
  });
});
